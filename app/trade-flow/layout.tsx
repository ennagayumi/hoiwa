import { createPageMetadata } from "@/lib/seo";

export const metadata = createPageMetadata("お取引の流れ｜肥料原料の調達から納品まで", "ご要望の確認、供給元選定、品質確認、見積り、契約、輸送・通関、納品、継続フォローまでの流れをご案内します。", "/trade-flow");

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
